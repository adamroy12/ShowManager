import javax.swing.*;
import java.awt.*;

public class ShowManager {

    public static void main(String[] args) {
        /*
        JFrame frame = new JFrame(); //creates a frame.
        frame.setTitle("Show Manager v.69"); // sets title of frame.
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setResizable(false); //prevents frame from being resized.
        frame.setSize(420,420); //sets x dimension and y dimension.
        frame.setVisible(true); // makes frame visible.

        ImageIcon image = new ImageIcon("logo.jpg"); //creates an imageIcon.
        frame.setIconImage(image.getImage()); //changes the icon of the frame.

        frame.getContentPane().setBackground(new Color(50, 150,255)); //change colour of the background.
         */

        //JLabel = a GUI display for a string of text, image or both.

        MyFrame myFrame = new MyFrame();

    }

}
